# Autentikáció beállítások
# auth_enabled = false → Localhost teszteléshez (nincs bejelentkezés)
# auth_enabled = true  → Production/VPS módhoz (bejelentkezés szükséges)
auth_enabled = false

[credentials]
username = "FxAdam"
password = "QW11ertz8899!!"
